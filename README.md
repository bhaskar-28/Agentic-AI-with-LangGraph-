# Agentic-AI-with-LangGraph-
A graph-based Agentic AI workflow project built with LangGraph, Python, and Streamlit. The system accepts a user query, plans execution steps, performs tool actions, validates outputs, and generates a structured final report using modular workflow nodes.
